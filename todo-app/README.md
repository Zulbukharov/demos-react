TODO app

```
* webpack запускает babel, которые 
```
```
* weppack собирает проект
```
```
* webpack после завершения работы babel дает сигнал браузеру
```
```
* объекты элементов в react максимально легковесны
```
```
* для объявления больших html элементов нужно загонять переменную в скобки создавая деревья
```
```
* reactDom => преобразует виртуальный DOM в настоящие объекты
```

```
* в JSX можно объявлять переменную в которой есть только 1 root элемент
* переменная внутри html объявляется использую {}
* внутри html можно использовать только строки, числа и булевые
* можно подставлять значения атрбитов
* class == className (jsx)
* for == htmlFor (jsx)
* webpack умеет собирать проект вместе, может находить js, css html файлы и склеить их вместе
```

